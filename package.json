{
  "name": "ai-coding-assessment-jobjen",
  "version": "1.0.0",
  "private": true,
  "description": "Candidate technical-assessment SPA (JupyterLite). Root build orchestration so one command installs every dependency (Python + Node) and produces a deployable bundle.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "install:all": "node scripts/install-all.mjs",
    "build": "node scripts/build-all.mjs",
    "dev": "npm --prefix react-app run dev",
    "preview": "npm --prefix react-app run preview"
  }
}
