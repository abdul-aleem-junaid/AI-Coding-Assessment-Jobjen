# Empty → the app uses the relative /api path, which the Vite dev proxy
# (vite.config.js) forwards to the backend. This avoids CORS in dev.
# Set an absolute origin here only if you want to bypass the proxy.
VITE_API_BASE_URL=
VITE_ENABLE_DEVTOOLS_GUARD=false
